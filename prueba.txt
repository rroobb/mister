FUNCION DECIMAL AREA(LISTA DECIMAL 2 w, DECIMAL h) {
	RETORNAR w.1 * h;
}

FUNCION ENTERO INICIO (ENTERO parametro1 , DECIMAL parametro2) {
	LISTA DECIMAL 2 x;
	DECIMAL y;
	ASIGNAR x.1 = 2.0;
	ASIGNAR y = AREA(x,3.0);
	IMPRIMIR(y);
	RETORNAR 0;

}