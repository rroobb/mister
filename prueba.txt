FUNCION DECIMAL AREA(DECIMAL w, DECIMAL h) {
	RETORNAR w * h;
}

FUNCION ENTERO INICIO (ENTERO parametro1 , DECIMAL parametro2) {
	DECIMAL z = 2.0;
	LISTA DECIMAL 2 x = [z,1.0];
	DECIMAL y;
	LISTA DECIMAL 2 m;
	ASIGNAR y = AREA(1.0,3.0);
	IMPRIMIR(y);
	ASIGNAR m = x;
	RETORNAR 0;

}