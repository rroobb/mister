FUNCION DECIMAL AREA(DECIMAL w, DECIMAL h) {
	RETORNAR w*h;
}

FUNCION ENTERO INICIO (ENTERO parametro1 , DECIMAL parametro2) {
	DECIMAL x;
	ENTERO num1 = 5;
	ENTERO num2 = 4;
	ASIGNAR num1 = num1 + num2;
	LEER(num2);
	ASIGNAR x = AREA(-3.0,5.0);
	RETORNAR 0;

}