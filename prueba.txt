FUNCION DECIMAL AREA(DECIMAL w, DECIMAL h) {
	RETORNAR w*h;
}

FUNCION DECIMAL AREA2(DECIMAL w, DECIMAL h, DECIMAL z) {
	DECIMAL x = 1.0;
	RETORNAR w/h;
}

FUNCION ENTERO INICIO (ENTERO parametro1 , DECIMAL parametro2) {
	DECIMAL x;
	LISTA ENTERO 4 milista;
	ENTERO y;
	ASIGNAR x = AREA(AREA(3.0+2.0,AREA(2.0,4.0)),5.0);
	ASIGNAR y = milista.2;
	IMPRIMIR(x);
	IMPRIMIR(1,1+1,3,2+2);
	RETORNAR 0;

}